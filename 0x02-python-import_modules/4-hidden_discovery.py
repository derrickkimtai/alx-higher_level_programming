#!/usr/bin/pytho3
import hidden_4
import types
def print_names():
    names = [name for name in dir(hidden_4) if not name.startswith('__')]
    name.sort()
    for name in names:
        print(name)
        if __name__ == '__main__';
        print_names()
